RODAR API: python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

RODAR APP:

npm install

npm install --legacy-peer-deps

npx expo run:android